# see dlptools::extract_wu_features

see dlptools::extract_wu_features

## Usage

``` r
add_wu_seg_state_bins(
  segs_df,
  state_bin_max = 5,
  bin_breaks = c(5e+06, 1e+07 + 1)
)
```

## Value

tibble
