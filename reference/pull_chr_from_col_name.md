# extract chromosome name from a bin name (chr_start_end)

an internal function for pulling information from a state matrix

## Usage

``` r
pull_chr_from_col_name(col_name)
```

## Arguments

- col_name:

  string of chr_start_end (column name in the internally used state
  dataframe)

## Value

string of the chromosome
