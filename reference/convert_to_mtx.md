# convert a dataframe to a wide matrix of bin states

convert a dataframe to a wide matrix of bin states

## Usage

``` r
convert_to_mtx(chr_df)
```

## Value

list of Y, the matrix, and cell_ord, the cell IDs in the matrix
