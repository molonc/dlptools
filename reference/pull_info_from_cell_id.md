# generic extractor of info contained in cell ids

generic extractor of info contained in cell ids

## Usage

``` r
pull_info_from_cell_id(cell_id, info = c(NA, "sample", "library"))
```

## Arguments

- cell_id:

  string or vector of cells id

- info:

  "sample" or "library" to pull

## Value

vector of requested information
