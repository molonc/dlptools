# confirms a vector is not numeric and is unorderd

returns TRUE if the vector is not numeric and is missing a defined
order. Used as a check of suitability for comparing events between tip
and parent nodes.

## Usage

``` r
is_not_numeric_lacks_order(vec)
```
