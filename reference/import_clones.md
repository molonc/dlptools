# read a clones t(c)sv file

read a clones t(c)sv file

## Usage

``` r
import_clones(clones_f)
```

## Arguments

- clones_f:

  a path to a t(c)sv file with cell_id and clone_id columns

## Value

tibble
