# read an annotations file (t(c)sv)

read an annotations file (t(c)sv)

## Usage

``` r
import_annotations_df(annotations_file)
```

## Arguments

- annotations_file:

  path to file. Requires at least sample_id column

## Value

tibble
