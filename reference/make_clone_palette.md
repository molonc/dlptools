# generate a color palette for clone labels

generate a color palette for clone labels

## Usage

``` r
make_clone_palette(clone_ids)
```

## Arguments

- clone_ids:

  unique vector of clone labels

## Value

named vector of hex colors, names are clone labels
