# creates a complex heatmap of a given matrix of states.

creates a complex heatmap of a given matrix of states.

## Usage

``` r
generate_state_hm(
  states_mat,
  phylo,
  labels_fontsize = 8,
  plot_cols = STATE_COLORS,
  left_annot = NULL,
  legend_11plus = FALSE
)
```
