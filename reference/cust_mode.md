# find most common element in a vector

For ties, just the first one in the list is returned.

## Usage

``` r
cust_mode(x, na_rm = FALSE)
```

## Arguments

- x:

  vector of values

## Value

most common element of input vector
