# read a tree from a file in newick format

read a tree from a file in newick format

## Usage

``` r
import_tree(tree_f)
```

## Arguments

- tree_f:

  a path to a tree file

## Value

phylo object
