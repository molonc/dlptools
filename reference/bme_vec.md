# picks beginning, middle, and end of a vector to handle when vectors that are too long are passed

picks beginning, middle, and end of a vector to handle when vectors that
are too long are passed

## Usage

``` r
bme_vec(vec, vec_name)
```
